# DoItTimer — Sprint 4 Quick Start

## Prereqs
- Node + pnpm installed
- Supabase project configured (env vars in `.env.local`)
- You already have Sprint 1–3 running.

## Run the app
```bash
pnpm install
pnpm dev
